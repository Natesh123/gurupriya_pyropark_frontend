#!/usr/bin/expect -f
set timeout -1
set password "Prasath@1990"
set ip "103.191.208.98"

puts "Checking apache..."
spawn ssh -o StrictHostKeyChecking=no root@$ip "ls /etc/apache2/sites-available/ && cat /etc/apache2/sites-available/*"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
