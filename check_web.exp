#!/usr/bin/expect -f
set timeout -1
set password "Prasath@1990"
set ip "103.191.208.98"

spawn ssh -o StrictHostKeyChecking=no root@$ip "which apache2 || which httpd || which litespeed"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
